# Objects

An object is any item you've saved to your mind — a URL, note, image, or document. Every object has a type, optional content, and metadata like tags, spaces, and AI-generated fields.

## The object model

| Property | Type | Description |
|----------|------|-------------|
| id | [Uid](types.md#uid) | Unique identifier for the object. |
| title | string | Display title. Set by the user, extracted from the source URL or content, or generated by AI. |
| summary | string | AI generated summary of the object. |
| content? | [Content](types.md#content) | The content body, if the object carries inline content. |
| blob? | [BlobReference](types.md#blobreference) | The underlying media for the object — present for uploaded files (images, video, PDFs) and similar binary content. Image blobs include a [Palette](types.md#palette) of dominant colors. |
| screenshot? | [BlobReference](types.md#blobreference) | Screenshot captured at save time — typically the rendered view of a saved web page. Bytes can also be fetched directly via [Get object screenshot](#get-object-screenshot). |
| entity? (Soon) | [Entity](entities.md) | The main entity. |
| spaces? | [ObjectSpace](#objectspace)[] | Spaces this object belongs to. |
| tags | [ObjectTag](#objecttag)[] | Tags associated with this object. |
| notes? | [ObjectNote](#objectnote)[] | Notes attached to this object. |
| source? | [ObjectSource](#objectsource) | The original source of the content. |
| bumped | [Timestamp](types.md#timestamp) | When the object was last bumped. Defaults to `created`, then updates whenever the same content is saved again — re-posting the URL, re-uploading the blob, or otherwise resolving to an existing object (see [Create an object](#create-an-object)). |
| created | [Timestamp](types.md#timestamp) | When the object was created. |
| modified | [Timestamp](types.md#timestamp) | When the object was last modified. |
| deleted? | [Timestamp](types.md#timestamp) | Present only when the object has been soft-deleted. Deleted objects remain available for 30 days — they're excluded from list and search results, can still be retrieved by ID, and are permanently destroyed when the window expires. See [Restore an object](#restore-an-object). |

### ObjectSource

| Property | Type | Description |
|----------|------|-------------|
| url | string | The original source URL. |

### ObjectSpace

| Property | Type | Description |
|----------|------|-------------|
| id | [Uid](types.md#uid) | The space's unique identifier. |

### ObjectNote

| Property | Type | Description |
|----------|------|-------------|
| id | [Uid](types.md#uid) | Unique identifier for the note. |
| content | [Content](types.md#content) | The note's body. |

### ObjectTag

| Property | Type | Description |
|----------|------|-------------|
| name | string | The tag label. |
| flags | [TagFlag](tags.md#tagflag) | How the tag was applied. |

## Actions

### List objects

Returns all objects belonging to the authenticated user. Objects outside the access key's [content scope](access-control.md#content-scope) are silently omitted from results.

```
GET /objects        1–250 credits
```

#### Query parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| q | string | — | Search query. See [syntax](search.md#query-syntax). |
| id | [Uid](types.md#uid) | — | Object IDs to fetch. Repeatable: `?id=…&id=…`. |
| spaceId | [Uid](types.md#uid) | — | Restrict results to objects belonging to the given [space](spaces.md). |
| similarTo (Mastermind) | [Uid](types.md#uid) | — | Return objects related to the given object ID, ranked by similarity. |
| contentAs | string | native | Format for `content` body. Currently `text/markdown`. |
| limit | integer | `10000` | Max results. Capped at `10000`. |

When `q` is provided, results are capped at `1000` and search costs apply.

#### Response 200 OK

```json
[
  {
    "id": "a1B2c3D4e5F6g7H8i9J0k1",
    "title": "The Art of Plain Text",
    "url": "https://example.com/plain-text",
    "content": {
      "type": "text/markdown",
      "body": "# The Art of Plain Text\n\nPlain text is the most portable, future-proof format..."
    },
    "tags": [
      { "name": "writing" },
      { "name": "tools" }
    ],
    "created": "2024-03-01T12:00:00Z",
    "modified": "2024-03-01T12:00:00Z",
    "bumped": "2024-03-01T12:00:00Z"
  }
]
```

### Create an object

Creates a new object from a URL, inline content, or uploaded file.

```
POST /objects        10–250 credits
```

#### Request body

| Property | Type | Description |
|----------|------|-------------|
| title | string | Display title. If omitted, it's extracted from the url or content. |
| spaces | [ObjectSpace](#objectspace)[] | Spaces to add the object to on creation. |
| tags | [ObjectTag](#objecttag)[] | Tags to attach on creation. |
| notes | object[] | Notes to attach on creation, each `{ "content": { "type": "application/prose+json", "body": <prose document> } }` — the same document format as [Create an object note](#create-an-object-note). |
| remoteId | string | Client-minted identity for this create (a UUID string), for retry deduplication — see [Response — repeated remoteId](#response--repeated-remoteid-existing-object-returned-200-ok). The server still mints the object's `id`; `remoteId` is correlated with it, never adopted as it. |
| blob (multipart only) | binary | Raw file bytes sent alongside the metadata. Only valid when the request is `multipart/form-data` — encode the metadata JSON as the `metadata` part and the bytes as the `blob` part. Capped at 64 MB. See [supported formats](supported-formats.md). |
| content | string \| [Content](types.md#content) | The content body. Pass a plain string or a structured Content object. |
| url | string | A remote URL to save. |

Provide exactly one of `blob`, `content`, or `url` — combining them returns 400.

#### Response 201 Created

```json
{
  "id": "a1B2c3D4e5F6g7H8i9J0k1",
  "title": "Example Article",
  "url": "https://example.com/article",
  "tags": [
    { "name": "reading" }
  ],
  "created": "2024-04-08T09:00:00Z",
  "modified": "2024-04-08T09:00:00Z",
  "bumped": "2024-04-08T09:00:00Z"
}
```

#### Response — duplicate, existing object bumped 200 OK

```json
{
  "id": "a1B2c3D4e5F6g7H8i9J0k1",
  "title": "Example Article",
  "url": "https://example.com/article",
  "tags": [
    { "name": "reading" }
  ],
  "created": "2024-04-08T09:00:00Z",
  "modified": "2024-04-08T09:00:00Z",
  "bumped": "2024-04-08T15:30:00Z"
}
```

If the request body resolves to a blob that already exists in your mind — the same URL, the same content body, or a byte-identical upload — the API returns the existing object instead of creating a duplicate, refreshes its `bumped` timestamp, and responds with `200 OK` rather than `201 Created`.

#### Response — repeated remoteId, existing object returned 200 OK

Repeating a create with the same `remoteId` returns the object the first attempt created, with `200 OK` rather than `201 Created` — whether or not the content would dedupe on its own. This is what makes a retried create safe for content that content-dedupe can't catch (two text bodies that differ, for example): a client that queues creates mints a UUID per queued item, sends it as `remoteId` on every attempt, and any attempt that reaches the server resolves onto the same object.

### Get an object

Retrieves a single object by its ID.

```
GET /objects/:id        1 credit
```

#### Path parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| id | [Uid](types.md#uid) | Object to retrieve. |

#### Query parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| contentAs | string | native | Format for `content` body. Currently `text/markdown`. |

#### Response 200 OK

```json
{
  "id": "a1B2c3D4e5F6g7H8i9J0k1",
  "title": "The Art of Plain Text",
  "url": "https://example.com/plain-text",
  "content": {
    "type": "text/markdown",
    "body": "# The Art of Plain Text\n\nPlain text is the most portable, future-proof format..."
  },
  "tags": [
    { "name": "writing" },
    { "name": "tools" }
  ],
  "created": "2024-03-01T12:00:00Z",
  "modified": "2024-03-01T12:00:00Z",
  "bumped": "2024-03-01T12:00:00Z"
}
```

### Get object blob

Returns the original blob that was uploaded to the object — no transcoded or derived variants. Only works for objects with a single uploaded attachment (e.g. an image, video, or PDF); returns `422` otherwise. The response may be a `302` redirect to a CDN URL, so your client must follow redirects.

```
GET /objects/:id/blob        1 credit
```

#### Path parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| id | [Uid](types.md#uid) | Object to fetch. |

#### Response 200 OK

```
<original blob bytes>
```

#### Response — redirect 302

```
Location: https://mymind.media/...
```

### Get object content

Returns the content of a text-based object. Set the `Accept` header to pick a format, or omit it to receive the content in its native format. Only works for text-based objects (e.g. `article`, `note`). Returns `422` for non-text types, and `406` if the requested format isn't supported.

```
GET /objects/:id/content        1 credit
```

#### Path parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| id | [Uid](types.md#uid) | Object to read. |

#### Headers

| Header | Description |
|--------|-------------|
| Accept | One of `text/markdown`, `application/prose+json`, or `text/html`. Omit to receive the content in its native format. |

#### Response 200 OK

```
# The Art of Plain Text

Plain text is the most portable, future-proof format for writing...
```

### Get object screenshot

Returns the screenshot captured at save time — typically the rendered view of a saved web page or article. Returns `422` for objects without a screenshot. The response may be a `302` redirect to a CDN URL, so your client must follow redirects.

```
GET /objects/:id/screenshot        1 credit
```

#### Path parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| id | [Uid](types.md#uid) | Object to fetch. |

#### Response 200 OK

```
<screenshot image bytes>
```

#### Response — redirect 302

```
Location: https://mymind.media/...
```

### Get object thumbnail

Returns a thumbnail image for the object. Useful for previews, lists, and grids. Available for any object with a renderable thumbnail (images, videos, articles with cover images, etc.).

```
GET /objects/:id/thumbnail        1 credit
```

#### Path parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| id | [Uid](types.md#uid) | Object to preview. |

#### Query parameters

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| size | string | full | Containment box as `WxH` (e.g. `100x100`). The thumbnail is scaled to fit inside this box, preserving aspect ratio — equivalent to CSS `object-fit: contain`. Omit for the default, pre-rendered thumbnail. |

#### Response 200 OK

```
<image bytes>
```

#### Response — redirect 302

```
Location: https://mymind.media/...
// signed URL, valid for ~5 minutes
```

### Update an object

Updates an object's metadata. Only include the fields you want to change — omitted fields are left untouched.

```
PATCH /objects/:id        2 credits
```

#### Path parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| id | [Uid](types.md#uid) | Object to update. |

#### Request body

| Property | Type | Description |
|----------|------|-------------|
| title | string | New display title for the object. |
| summary | string | New summary for the object. |

#### Response 200 OK

```json
{}
```

### Create an object note

Appends a new note to the object's `notes` array and returns its assigned `id`. Send Markdown for a simple note, or [Prose](prose.md) to preserve rich formatting.

**UI support**

Objects can hold up to 100 notes via the API, but the mymind app currently surfaces only the first one (`notes[0]`). Additional notes are stored and returned by the API, but won't be visible in the app until multi-note UI ships.

```
POST /objects/:objectId/notes        10 credits
```

#### Path parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| objectId | [Uid](types.md#uid) | Object to attach the note to. |

#### Headers

| Header | Description |
|--------|-------------|
| Content-Type (required) | Either `text/markdown` or `application/prose+json`. |

#### Request body

The note body, in the format declared by `Content-Type`.

##### Request — Markdown

```markdown
Reminders for later:

- [ ] Follow up with Sam
- [ ] Check on the proofs
```

##### Request — Prose

```json
{
  "type": "doc",
  "content": [
    {
      "type": "paragraph",
      "content": [{ "type": "text", "text": "Reminders for later:" }]
    },
    {
      "type": "taskList",
      "content": [
        {
          "type": "taskItem",
          "attrs": { "checked": false },
          "content": [
            {
              "type": "paragraph",
              "content": [{ "type": "text", "text": "Follow up with Sam" }]
            }
          ]
        },
        {
          "type": "taskItem",
          "attrs": { "checked": false },
          "content": [
            {
              "type": "paragraph",
              "content": [{ "type": "text", "text": "Check on the proofs" }]
            }
          ]
        }
      ]
    }
  ]
}
```

#### Response 201 Created

```json
{
  "id": "n4K8m2N6p9Q3r5T7v1X0z2"
}
```

### Update an object note

Replaces the body of an existing note. Returns `404` if the note doesn't exist on the object. This is a full replace — not a patch.

```
PUT /objects/:objectId/notes/:noteId        Idempotent        10 credits
```

#### Path parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| objectId | [Uid](types.md#uid) | Object the note belongs to. |
| noteId | [Uid](types.md#uid) | Note to update. |

#### Headers

| Header | Description |
|--------|-------------|
| Content-Type (required) | Either `text/markdown` or `application/prose+json`. |

#### Response 200 OK

```json
{}
```

### Delete an object note

Removes a note from the object's `notes` array. Idempotent — deleting a note that's already gone is a no-op.

```
DELETE /objects/:objectId/notes/:noteId        Idempotent        1 credit
```

#### Path parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| objectId | [Uid](types.md#uid) | Object the note belongs to. |
| noteId | [Uid](types.md#uid) | Note to delete. |

#### Response 200 OK

```json
{}
```

### Update object content

Replaces the entire content body of a Note. Returns `422` for any other object type. This is a full replace — not a patch. Send Markdown for a simple rewrite, or [Prose](prose.md) to preserve rich formatting.

```
PUT /objects/:id/content        Idempotent        20 credits
```

#### Path parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| id | [Uid](types.md#uid) | Object to update. |

#### Headers

| Header | Description |
|--------|-------------|
| Content-Type (required) | Either `text/markdown` or `application/prose+json`. |

#### Response 200 OK

```json
{}
```

### Add tags to an object

Adds tags to an object.

```
POST /objects/:objectId/tags        Idempotent        2 credits
```

#### Path parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| objectId | [Uid](types.md#uid) | Object to tag. |

#### Request body

| Property | Type | Description |
|----------|------|-------------|
| tags | [ObjectTag](#objecttag)[] | Tags to add to the object. |

##### Request

```json
{
  "tags": [
    { "name": "design" },
    { "name": "inspiration" }
  ]
}
```

#### Response 200 OK

```json
{}
```

### Remove tags from an object

Removes one or more tags from an object. Tags can be referenced by `name` or by `id` — mix and match within the same request. Removing a tag that isn't on the object is a no-op.

```
DELETE /objects/:objectId/tags        Idempotent        2 credits
```

#### Path parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| objectId | [Uid](types.md#uid) | Object to untag. |

#### Request body

An array of tag references. Each entry must contain either `name` or `id`.

##### Request — by name

```json
[
  { "name": "design" },
  { "name": "inspiration" }
]
```

##### Request — by id

```json
[
  { "id": "t8R3p9K2m5N7q1V4x6L0z8" }
]
```

#### Response 200 OK

```json
{}
```

### Add an object to spaces

Adds an object to one or more spaces. Objects may belong to a maximum of 100 spaces.

```
POST /objects/:objectId/spaces        Idempotent        2 credits
```

#### Path parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| objectId | [Uid](types.md#uid) | Object to add. |

#### Request body

##### Request

```json
[
  { "id": "j5K6l7M8n9O0p1Q2r3S4t5" }
]
```

#### Response 200 OK

```json
{}
```

### Pin an object

Pins an object to your top of mind. Pass an optional `position` in the body to control ordering — omit it to append to the end.

```
POST /objects/:id/pin        3 credits
```

#### Path parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| id | [Uid](types.md#uid) | Object to pin. |

#### Request body

| Property | Type | Description |
|----------|------|-------------|
| position | number | Zero-based slot in your top of mind. Omit to append to the end. |

#### Response 200 OK

```json
{}
```

### Unpin an object

Removes an object from your top of mind. Idempotent — unpinning an object that isn't pinned is a no-op.

```
DELETE /objects/:id/pin        Idempotent        3 credits
```

#### Path parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| id | [Uid](types.md#uid) | Object to unpin. |

#### Response 200 OK

```json
{}
```

### Delete an object

Soft-deletes an object. Deleted objects are recoverable for 30 days, after which they are permanently destroyed.

```
DELETE /objects/:id        Idempotent        1 credit
```

#### Path parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| id | [Uid](types.md#uid) | Object to delete. |

#### Response 200 OK

```json
{}
```

### Restore an object

Restores a deleted object. Objects can be restored within 30 days of deletion.

```
POST /objects/:id/restore        Idempotent        1 credit
```

#### Path parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| id | [Uid](types.md#uid) | Object to restore. |

#### Response 200 OK

```json
{}
```

## Examples

### List objects

#### All

```
GET /objects?limit=1000
```

#### By ID

```
GET /objects?id=a1B2c3D4e5F6g7H8i9J0k1&id=z9Y8x7W6v5U4t3S2r1Q0p9
```

#### By Query

```
GET /objects?q=action%3Aread%20%26%26%20completed%3Afalse
```

### Create an object

#### Note

```
POST /objects
Content-Type application/json

{
  "content": {
    "type": "text/markdown",
    "body": "# Meeting notes\n\n- Kickoff with Sam\n- Follow up Thursday"
  },
  "tags": [
    { "name": "work" }
  ]
}
```

#### URL

```
POST /objects
Content-Type application/json

{
  "url": "https://example.com/article",
  "tags": [
    { "name": "reading" }
  ]
}
```

#### Image

```
POST /objects
Content-Type multipart/form-data

--boundary
Content-Disposition: form-data; name="metadata"
Content-Type: application/json

{
  "title": "Sunset",
  "spaces": [{ "id": "sP7k4M2n8Q3r5V9x1L6y0T" }],
  "tags": [{ "name": "travel" }]
}
--boundary
Content-Disposition: form-data; name="blob"; filename="sunset.jpg"
Content-Type: image/jpeg
Content-Length: 248532

<binary file contents>
--boundary--
```

[See supported formats →](supported-formats.md)

### Get an object

```
GET /objects/:id
```

### Get object content

#### Markdown

```
GET /objects/:id/content
Accept text/markdown
```

##### Response 200 OK

```markdown
# The Art of Plain Text

Plain text is the most portable, future-proof format for writing...
```

#### Prose

```
GET /objects/:id/content
Accept application/prose+json
```

##### Response 200 OK

```json
{
  "type": "doc",
  "content": [
    {
      "type": "heading",
      "attrs": { "level": 1 },
      "content": [{ "type": "text", "text": "The Art of Plain Text" }]
    },
    {
      "type": "paragraph",
      "content": [
        { "type": "text", "text": "Plain text is the most portable..." }
      ]
    }
  ]
}
```

#### HTML

```
GET /objects/:id/content
Accept text/html
```

##### Response 200 OK

```html
<h1>The Art of Plain Text</h1>
<p>Plain text is the most portable, future-proof format for writing...</p>
```

### Update an object

```
PATCH /objects/:id
Content-Type application/json

{
  "title": "Updated title"
}
```

### Create an object note

#### Markdown

```
POST /objects/:objectId/notes
Content-Type text/markdown

Reminders for later:

- [ ] Follow up with Sam
- [ ] Check on the proofs
```

#### Prose

```
POST /objects/:objectId/notes
Content-Type application/prose+json

{
  "type": "doc",
  "content": [
    {
      "type": "paragraph",
      "content": [{ "type": "text", "text": "Reminders for later:" }]
    },
    {
      "type": "taskList",
      "content": [
        {
          "type": "taskItem",
          "attrs": { "checked": false },
          "content": [
            {
              "type": "paragraph",
              "content": [{ "type": "text", "text": "Follow up with Sam" }]
            }
          ]
        },
        {
          "type": "taskItem",
          "attrs": { "checked": false },
          "content": [
            {
              "type": "paragraph",
              "content": [{ "type": "text", "text": "Check on the proofs" }]
            }
          ]
        }
      ]
    }
  ]
}
```

### Update an object note

```
PUT /objects/:objectId/notes/:noteId
Content-Type text/markdown

Reminders for later:

- [x] Follow up with Sam
- [ ] Check on the proofs
```

### Delete an object note

```
DELETE /objects/:objectId/notes/:noteId
```

### Update object content

#### Markdown

```
PUT /objects/:id/content
Content-Type text/markdown

# New heading

Updated note body...
```

#### Prose

```
PUT /objects/:id/content
Content-Type application/prose+json

{
  "type": "doc",
  "content": [
    {
      "type": "heading",
      "attrs": { "level": 1 },
      "content": [{ "type": "text", "text": "New heading" }]
    }
  ]
}
```

### Add tags to an object

```
POST /objects/:objectId/tags
Content-Type application/json

[
  { "name": "reading" },
  { "name": "tools" }
]
```

### Remove tags from an object

```
DELETE /objects/:objectId/tags
Content-Type application/json

[
  { "name": "reading" }
]
```

### Add an object to spaces

```
POST /objects/:objectId/spaces
Content-Type application/json

[
  { "id": "sP7k4M2n8Q3r5V9x1L6y0T" }
]
```

### Pin an object

```
POST /objects/:id/pin
Content-Type application/json

{
  "position": 0
}
```

### Unpin an object

```
DELETE /objects/:id/pin
```

### Delete an object

```
DELETE /objects/:id
```

### Restore an object

```
POST /objects/:id/restore
```
